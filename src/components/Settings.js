import React from "react";
import Button from "./Button";

const Settings = () => {
  return (
    <div>
      <p>Settings</p>
      <Button text="Logout" />
    </div>
  );
};

export default Settings;
